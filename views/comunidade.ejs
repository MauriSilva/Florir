<%- include("partials/layout") %>
  <%- include("partials/header") %>

    <main class="container">

      <section class="text-center">
        <h1>🌼 Comunidade Florir</h1>
        <p>Aqui você encontra contatos importantes e pode deixar sua mensagem para o projeto.</p>

        <!-- CONTATOS ÚTEIS -->
        <div class="features-grid" style="margin-top: 2rem;">
          <div class="feature-card">
            <i class="fa-solid fa-phone-volume"></i>
            <h3>CVV – 188</h3>
            <p>Atendimento gratuito e sigiloso 24h.</p>
            <a href="https://www.cvv.org.br" target="_blank" class="btn">Acessar site</a>
          </div>

          <div class="feature-card">
            <i class="fa-solid fa-truck-medical"></i>
            <h3>SAMU – 192</h3>
            <p>Emergências médicas.</p>
          </div>

          <div class="feature-card">
            <i class="fa-solid fa-notes-medical"></i>
            <h3>Disque Saúde – 136</h3>
            <p>Informações sobre serviços de saúde pública.</p>
          </div>

          <div class="feature-card">
            <i class="fa-solid fa-map-location-dot"></i>
            <h3>Mapa da Saúde</h3>
            <p>Encontre hospitais, clínicas e ONGs perto de você.</p>
            <a href="/mapas" class="btn">Abrir mapa</a>
          </div>
        </div>
      </section>

      <hr style="border: 0; border-top: 1px solid rgba(0,0,0,0.1); margin: 2rem 0;">

      <!-- COMENTÁRIOS -->
      <section>
        <div class="glass-card" style="max-width: 800px; margin: 0 auto;">
          <h2 class="text-center">💬 Deixe seu comentário</h2>
          <form action="/comunidade" method="POST">
            <div class="form-group">
              <label>Seu nome:</label>
              <input type="text" name="name" required placeholder="Como você gostaria de ser chamada?">
            </div>
            <div class="form-group">
              <label>Sua mensagem:</label>
              <textarea name="message" required placeholder="Escreva sua mensagem aqui..."></textarea>
            </div>
            <button type="submit" class="btn" style="width: 100%;">Enviar comentário</button>
          </form>
          <p style="margin-top: 10px; font-size: 0.9rem; color: var(--text-muted); text-align: center;">
            <i class="fa-solid fa-circle-info"></i> Seu comentário será enviado para aprovação antes de aparecer aqui.
          </p>
        </div>

        <h3 class="text-center" style="margin-top: 3rem;">Comentários da Comunidade</h3>

        <div style="display: flex; flex-direction: column; gap: 1rem; max-width: 800px; margin: 2rem auto;">
          <% if (comments.length===0) { %>
            <p class="text-center">Nenhum comentário ainda. Seja o primeiro a participar!</p>
            <% } else { %>
              <% comments.forEach(c=> { %>
                <div class="glass-card" style="padding: 1.5rem;">
                  <div
                    style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 0.5rem;">
                    <strong style="color: var(--primary); font-size: 1.1rem;">
                      <%= c.name %>
                    </strong>
                    <small style="color: var(--text-muted);">
                      <%= c.createdAt.toLocaleString("pt-BR") %>
                    </small>
                  </div>
                  <p style="margin-bottom: 0;">
                    <%= c.message %>
                  </p>

                  <% if (user && user.isAdmin) { %>
                    <form action="/comunidade/deletar/<%= c.id %>" method="POST"
                      style="margin-top: 1rem; text-align: right;">
                      <button class="btn" style="background: #e74c3c; padding: 0.4rem 0.8rem; font-size: 0.8rem;"
                        onclick="return confirm('Excluir este comentário?')">
                        Excluir
                      </button>
                    </form>
                    <% } %>
                </div>
                <% }) %>
                  <% } %>
        </div>
      </section>

    </main>

    <%- include("partials/footer") %>