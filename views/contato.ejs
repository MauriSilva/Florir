<%- include("partials/layout") %>
  <%- include("partials/header") %>

    <main class="container">

      <% if (typeof enviado !=='undefined' && enviado) { %>
        <div class="glass-card"
          style="background: #d4edda; color: #155724; border-color: #c3e6cb; text-align: center; margin-top: 2rem;">
          <h3>Mensagem enviada com sucesso! 💖</h3>
          <p>Obrigada por entrar em contato. Responderemos em breve.</p>
        </div>
        <script>
          setTimeout(() => {
            window.location.href = "/contato";
          }, 4000);
        </script>
        <% } %>

          <section style="display: flex; justify-content: center; align-items: center; min-height: 60vh;">
            <div class="glass-card" style="width: 100%; max-width: 600px;">
              <h2 class="text-center">Fale Conosco</h2>
              <p class="text-center">Adoraríamos ouvir o que você tem a dizer!</p>

              <form action="/contato" method="POST">
                <div class="form-group">
                  <label>Nome:</label>
                  <input type="text" name="nome" required placeholder="Seu nome completo">
                </div>

                <div class="form-group">
                  <label>Email:</label>
                  <input type="email" name="email" required placeholder="seu@email.com">
                </div>

                <div class="form-group">
                  <label>Mensagem:</label>
                  <textarea name="mensagem" required placeholder="Como podemos ajudar?"></textarea>
                </div>

                <button type="submit" class="btn" style="width: 100%;">Enviar Mensagem</button>
              </form>
            </div>
          </section>

    </main>

    <%- include("partials/footer") %>